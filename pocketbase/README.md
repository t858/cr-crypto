# PocketBase Backend Folder

Place your `pocketbase.exe` executable inside this folder (`pocketbase/`).

### How to Run:
```bash
npm run pb
```
Or manually from terminal:
```bash
cd pocketbase
./pocketbase.exe serve
```

Admin dashboard will be available at:
`http://127.0.0.1:8090/_/`
